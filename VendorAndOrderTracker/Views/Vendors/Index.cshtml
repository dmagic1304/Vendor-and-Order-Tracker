@{
  Layout = "_Layout";
}
@using VendorAndOrderTracker.Models;

<h2>Vendors List</h2>

@if(@Model.Count == 0)
{
  <h3>There are no existing vendors</h3>
}

<ul>
@foreach (Vendor vendor in @Model)
{
  <li>@Model.Title</li>
}
</ul>